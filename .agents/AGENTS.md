# Panvas Development Rules

- Do not use placeholder implementations or TODOs.
- Implement production-ready CRUD handlers with proper validation and error handling.
- Ensure every domain API has corresponding load, save, rename, move, and delete operations where applicable.
- Keep the UI unchanged; only replace mock data with real persistence.
